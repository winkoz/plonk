---
kind: pipeline
type: kubernetes
name: default

steps:
  - name: restore-cache-with-filesystem
    image: drillster/drone-volume-cache
    settings:
      restore: true
      mount:
        - ./go-bin
    volumes:
    - name: cache
      path: /cache
  
  - name: build_and_test
    image: golang:1.15.6
    commands:
    # If no cache found with the tools, download them
    - "[ ! -f go-bin/go-bindata ] && go get -u gotest.tools/gotestsum"
    - "[ ! -f go-bin/gotestsum ] && go get -u github.com/go-bindata/go-bindata/..."
    # Make a go-bin directory if none exists
    - mkdir -p go-bin
    # Copy tools binaries into the local bin path
    - "[ ! -f go-bin/go-bindata ] && cp $GOPATH/bin/go-bindata go-bin/."
    - "[ ! -f go-bin/gotestsum ] && cp $GOPATH/bin/gotestsum go-bin/."
    # Builds & Tests the code
    - make go-build GO-BIN-FOLDER=go-bin/
    - make go-test GO-BIN-FOLDER=go-bin/

  - name: rebuild-cache-with-filesystem
    image: drillster/drone-volume-cache
    settings:
      rebuild: true
      mount:
        - ./go-bin
    volumes:
    - name: cache
      path: /cache

  - name: smoke-Tests
    image: docker:20.10.5-dind
    commands:
    - apk add --no-cache make
    - make smoke-tests

volumes:
- name: cache
  host:
    path: /tmp/drone/cache

---
kind: signature
hmac: aa8c62243dc7dfddbac9205c624abdeec04c070c48e32128ecc144b887da43d6

...
