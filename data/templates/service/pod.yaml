apiVersion: v1
kind: Pod
metadata:
  name: {{.NAME}}-{{.ENV}}-pod
  labels:
    app: {{.NAME}}-{{.ENV}}
spec:
  containers:
  - name: {{.NAME}}-{{.ENV}}
    image: {{.DOCKER_IMAGE}}
    ports:
    - containerPort: {{.CONTAINER_PORT}}