apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{.NAME}}-{{.ENV}}-service-account
  namespace: kube-system
  labels:
    app: {{.NAME}}-{{.ENV}}
secrets: {{range $k, $v := .ENVIRONMENT_SECRETS}} 
  - name: {{$k}} 
    value: "{{$v | title}}"
{{end}}