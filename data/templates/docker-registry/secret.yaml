apiVersion: v1
kind: Secret
metadata:
  name: {{.NAME}}-{{.ENV}}-secret
  labels:
    app: {{.NAME}}-{{.ENV}}
type: Opaque
data:
  htpasswd: |
{{ .REGISTRY_HTPASSWD_PATH | readFile | base64Encode }}

