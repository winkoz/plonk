apiVersion: v1
kind: Secret
metadata:
  name: {{.NAME}}-{{.ENV}}-secret
  namespace: {{.NAME}}-{{.ENV}}
  labels:
    app: {{.NAME}}-{{.ENV}}
type: Opaque
data:
  htpasswd: {{ .REGISTRY_HTPASSWD_PATH | readFile | base64Encode }}
