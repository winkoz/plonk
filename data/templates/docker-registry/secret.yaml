apiVersion: v1
kind: Secret
metadata:
  name: {{.NAME}}-{{.ENV}}-secret
  labels:
    app: {{.NAME}}-{{.ENV}}
  annotations:
    digitalocean.com/dosecret-identifier:
type: kubernetes.io/dockerconfigjson
data:
  .dockercfg: |
{{ .DOCKER_CONFIG_PATH | readFile | base64Encode }}

