apiVersion: v1
kind: Service
metadata:
  name: {{.NAME}}-{{.ENV}}-service
  namespace: {{.NAME}}-{{.ENV}}
  labels:
    app: {{.NAME}}-{{.ENV}}
spec:
  type: ClusterIP
  ports:
  - port: {{.SERVICE_PORT}}
    targetPort: {{.CONTAINER_PORT}}
  selector:
    app: {{.NAME}}-{{.ENV}}
