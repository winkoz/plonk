---
apiVersion: v1
kind: Service
metadata:
  name: {{.NAME}}-{{.ENV}}-{{.COMPONENT}}-service
  namespace: {{.NAME}}-{{.ENV}}
  labels:
    app: {{.NAME}}-{{.ENV}}
    component: {{.COMPONENT}}
spec:
  type: ClusterIP
  ports:
    - port: {{.POSTGRES_PORT}}
  selector:
    app: {{.NAME}}-{{.ENV}}