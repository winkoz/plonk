apiVersion: v1
kind: ConfigMap
metadata:
  namespace: {{.NAME}}-{{.ENV}}
  name: {{.NAME}}-{{.ENV}}-config-map
  labels:
    app: {{.NAME}}-{{.ENV}}
    component: {{.COMPONENT}}
data:
  {{.ALERT_MANAGER_FILES_FILE_PATH | readFileToStr}}